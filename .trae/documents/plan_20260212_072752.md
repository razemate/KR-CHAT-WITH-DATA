I will launch the Vanna application in a mock configuration, allowing you to explore the interface without requiring external API keys.

### Implementation Steps
1.  **Install Dependencies**: Navigate to the `vanna` directory and install the package with FastAPI support.
2.  **Run Mock Server**: Start the built-in mock server example.
3.  **Preview Application**: Open the web interface to verify it's running.